# SouthGrid 待办

## 已完成（文档本轮）

- README / 安装链路按 `g1_lerobot` 的 OrcaLab **7.1 / 26.7.1** 对齐
- 宇树采集文档改为 OSC + Pico 8001 + `joint_strip`
- 删除文档里已不存在的 IK / TeleVuer / `eval_g1_pick_lerobot.py` 命令

## 仍未做

1. **宇树 OSC 推理脚本**：采集已是 18 维末端位姿，没有对应 eval。
2. **智元采集/推理正文**：除 7.1 版本号外未改流程（与代码改动无关）。
3. **`uni_osc.json` 相机端口** 与脚本默认值可能不一致，测试时按文档手动改端口。
4. 左臂锁定姿态仍是代码里的侧平举，没有改成自然下垂。
